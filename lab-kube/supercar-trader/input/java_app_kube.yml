---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: sconrod/tomcat
    tag: '1'

inputs:
  - name: input
run:
  path: /bin/sh
  args:
    - -ce
    - |
      cd input
      export AWS_PAGER=""


params:
  VAULT_ADDR: ((vault.addr))
  SSH_TOKEN: ((ssh-token.token))
